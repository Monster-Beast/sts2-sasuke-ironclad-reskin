# Public-beta runtime binding review

The two real observation sessions for Steam public-beta build 24251656 have been manually reviewed. Ten bindings are approved only as exact-build canary candidates. `form_removed` and `character_state` remain blocked pending targeted evidence. The production integration contract and runtime profile remain disabled.
