# 卡牌释放动作与 VFX 规范

动作系统只响应视觉事件，不接管伤害、格挡、状态、目标选择或行动队列。

## 通用事件

`windup_start` → `cast_or_dash` → `impact` → `spawn_vfx` → `recover_start` → `complete`

## 时长

- 快速：0.40–0.70 秒；
- 标准：0.65–0.95 秒；
- 重型：0.90–1.25 秒；
- 形态：1.20–1.80 秒。

原游戏动作更短时，以原时序为上限。普通攻击不使用全屏震动；禁止连续高频白闪；所有临时节点必须在 `complete` 清理。
